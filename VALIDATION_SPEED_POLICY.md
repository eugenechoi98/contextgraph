# VALIDATION_SPEED_POLICY

## 验证分层

### L1：快速验证

- 适用于文档、配置、小范围逻辑改动
- 执行目标模块单测或最小 smoke test

### L2：主链路验证

- 适用于索引、检索、API、CLI 等核心链路改动
- 至少覆盖一次建库、一次索引、一次检索、一次 trace 查询

### L3：高风险验证

- 适用于数据库 schema 变更、缓存策略调整、共享底层能力变更
- 必须扩大到受影响流程，并记录残余风险

## 提速策略

- 优先跑受影响模块测试
- 先做只读 smoke test，再做正式验证
- 大文件输出必须裁剪，避免无意义上下文膨胀

## 磁盘与 DB 安全规则

- 默认数据库写入 `.data/`
- 做 schema 变更前先确认影响面
- 禁止未经确认删除现有索引文件
- 批量生成临时文件时，优先复用已有目录并在任务结束后清理

